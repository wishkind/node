import slide from './slide/index';

const prototypes = {
  slide,
};

const extendedDefaults = {}

export default class Swiper {
  constructor(...args) {
    let el;
    let params;
    alert(args[1].direction);
  }
}
